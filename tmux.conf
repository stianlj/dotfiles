set -g status-interval 1
set -ga terminal-overrides ",xterm-256color*:Tc"
# Undercurl
# set -g default-terminal "${TERM}"
set -g default-terminal "tmux-256color"
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0
set -g update-environment 'DISPLAY WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP'

set-option -g default-shell /usr/bin/fish

set -g prefix C-Space
unbind-key C-b
bind-key C-Space send-prefix

set -g base-index 1
set -g pane-base-index 1

set -g mouse on

setw -g mode-keys vi
setw -g aggressive-resize on

set -s escape-time 0

bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"
bind C-j display-popup -E "tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse | xargs tmux switch-client -t"

is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n C-h if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n C-j if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n C-k if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n C-l if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'

bind-key -n M-S-h if-shell "$is_vim" 'send-keys M-S-h' 'resize-pane -L 3'
bind-key -n M-S-j if-shell "$is_vim" 'send-keys M-S-j' 'resize-pane -D 3'
bind-key -n M-S-k if-shell "$is_vim" 'send-keys M-S-k' 'resize-pane -U 3'
bind-key -n M-S-l if-shell "$is_vim" 'send-keys M-S-l' 'resize-pane -R 3'

tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}",
bind "\\" split-window -v -p 35 -c "#{pane_current_path}"
bind '|' split-window -h -p 40 -c "#{pane_current_path}",
bind 'c' new-window -c "#{pane_current_path}"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'catppuccin/tmux'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'MunifTanjim/tmux-mode-indicator'
set -g @plugin 'olimorris/tmux-pomodoro-plus'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @pomodoro_notifications 'on'

set -g @resurrect-strategy-nvim 'session'
set -g @continuum-boot 'on'
set -g @continuum-restore 'on'

# set -g @catppuccin_window_tabs_enabled on
# set -g @catppuccin_right_separator "█"
# set -g @catppuccin_left_separator "█"


bg="#2e3440"
fg="#ECEFF4"
red="#bf616a"
orange="#d08770"
yellow="#ebcb8b"
blue="#5e81ac"
lblue="#6c86a1"
green="#a3be8c"
cyan="#88c0d0"
magenta="#b48ead"
pink="#FFA19F"
grey1="#f8fafc"
grey2="#f0f1f4"
grey3="#eaecf0"
grey4="#d9dce3"
grey5="#c4c9d4"
grey6="#b5bcc9"
grey7="#929cb0"
grey8="#8e99ae"
grey9="#74819a"
grey10="#616d85"
grey11="#464f62"
grey12="#3a4150"
grey13="#333a47"
grey14="#242932"
grey15="#1e222a"
grey16="#1c1f26"
grey17="#0f1115"
grey18="#0d0e11"
grey19="#020203"

mode_separator=""
# set -g @mode_indicator_prefix_prompt " WAIT #[default]#[fg=$green,bg=$blue] $mode_separator"
# set -g @mode_indicator_prefix_mode_style bg=$green,fg=$fg
# set -g @mode_indicator_copy_prompt " COPY #[default]#[fg=$blue,bg=$blue] $mode_separator"
# set -g @mode_indicator_copy_mode_style bg=$blue,fg=$fg
# set -g @mode_indicator_sync_prompt " SYNC #[default]#[fg=$red,bg=$blue] $mode_separator"
# set -g @mode_indicator_sync_mode_style bg=$red,fg=$fg
# set -g @mode_indicator_empty_prompt " TMUX #[default]#[fg=$grey12,bg=$blue] $mode_separator"
# set -g @mode_indicator_empty_mode_style bg=$grey12,fg=$grey7
set -g @mode_indicator_prefix_prompt " WAIT #[default]#[fg=$green]$mode_separator"
set -g @mode_indicator_prefix_mode_style bg=$green,fg=$fg
set -g @mode_indicator_copy_prompt " COPY #[default]#[fg=$blue]$mode_separator"
set -g @mode_indicator_copy_mode_style bg=$blue,fg=$fg
set -g @mode_indicator_sync_prompt " SYNC #[default]#[fg=$red]$mode_separator"
set -g @mode_indicator_sync_mode_style bg=$red,fg=$fg
set -g @mode_indicator_empty_prompt " TMUX #[default]#[fg=$grey12]$mode_separator"
set -g @mode_indicator_empty_mode_style bg=$grey12,fg=$grey7

# set -g status on
# set -g status-justify centre
# set -g status-position bottom
# set -g status-left-length 90
# set -g status-right-length 90
# set -g status-style "bg=$bg"
# set -g window-style ""
# set -g window-active-style ""

set -g message-style "fg=#232831,bg=#abb1bb"
set -g message-command-style "fg=#232831,bg=#abb1bb"
set -g pane-border-style "fg=#abb1bb"
set -g pane-active-border-style "fg=#81a1c1"
set -g status-style "fg=#81a1c1,bg=#232831"

setw -g window-status-style "NONE,fg=#7e8188,bg=#232831"
setw -g window-status-activity-style "underscore,fg=#7e8188,bg=#232831"

# set -g message-style bg=$magenta,fg=$bg
# set -g pane-border-style fg=$grey11
# set -g pane-active-border-style fg=$magenta
# setw -g window-status-separator ""

#################################### FORMAT ####################################
set -g status-left "#{tmux_mode_indicator}"
# set -g status-right "#[fg=$grey11]%R"
# comp.separator = left and { right = '' } or { left = '' }
# section_separators = { left = '', right = '' },
# setw -g window-status-format "#[fg=$grey11,nobold,nounderscore,noitalics] #[fg=$grey11] #I #W #[fg=$grey11,nobold,nounderscore,noitalics]"
# setw -g window-status-current-format "#[fg=$grey11,nobold,nounderscore,noitalics] #[fg=$magenta] #I #W #[fg=$grey11,nobold,nounderscore,noitalics]"
setw -g window-status-format "#[fg=#232831,bg=#232831,nobold,nounderscore,noitalics]#[default] #I #W #F #[fg=#232831,bg=#232831,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=#232832,bg=#abb1bb,nobold,nounderscore,noitalics]#[fg=#232831,bg=#abb1bb,bold] #I \ #W #F #[fg=#abb1bb,bg=#232831,nobold,nounderscore,noitalics]"

# set -g status-left "#{tmux_mode_indicator}#[fg=#232831,bg=#81a1c1,bold] #S #[fg=#81a1c1,bg=#232831,nobold,nounderscore,noitalics]"
# set -g status-right "#{pomodoro_status} #[fg=$grey11]%R"
set -g status-right ''


run '~/.tmux/plugins/tpm/tpm'
