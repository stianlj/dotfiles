#!/usr/bin/env bash

# TODO: Weird ass 'Cannot find file %u' This is in the command. Just replace it
# Make a list with both pretty name, command and placement

# { ls -d /usr/share/applications/* ; ls -d ~/.local/share/applications/* ; } |\
find /usr/share/applications ~/.local/share/applications -type f -name \*.desktop |\
  awk '{$1}'
  # fzf
  # xargs grep '^Exec' |\
  # tail -1 |\
  # sed 's/^Exec=//' |\
  # cat
  # { sh <&3 3<&- & } 3<&0
